---
'@iwsdk/vite-plugin-dev': patch
---

Allow an explicitly configured local example-asset directory through Vite's
file-system boundary while preserving the default workspace root and denying
unrelated external paths.
