---
'@iwsdk/create': patch
'@iwsdk/vite-plugin-dev': patch
---

Light scaffolded AR scenes by default, restore the workspace button that opens
the runtime in the default browser, and return an actionable error when an
editor command requires a scene to be open.
