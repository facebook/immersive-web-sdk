---
'@iwsdk/create': patch
---

Use the bundled UIKit default font in generated starter panels instead of
requiring runtime downloads from Google Fonts. Canonical examples now follow
the same network-font-free pattern.
